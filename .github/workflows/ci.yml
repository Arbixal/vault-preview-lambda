name: CI

on:
  push:
    branches:
      - master
  pull_request:
    branches:
      - master

permissions:
  contents: read

jobs:
  build:
    name: Build solution
    runs-on: ubuntu-24.04

    steps:
      - name: Check out repository
        uses: actions/checkout@v7

      - name: Set up .NET
        uses: actions/setup-dotnet@v6
        with:
          dotnet-version: 10.0.x

      - name: Set up Node.js
        uses: actions/setup-node@v5
        with:
          node-version: 22.x
          cache: npm
          cache-dependency-path: contracts/v1/fixtures/package-lock.json

      - name: Restore dependencies
        run: dotnet restore VaultPreviewLambda.sln

      - name: Validate contract fixtures
        working-directory: contracts/v1/fixtures
        run: npm ci && npm run validate

      - name: Validate SAM templates
        run: node -e "const fs=require('fs'); for (const path of ['src/VaultPreviewLambda/serverless.template','src/CharacterDataLambda/serverless.template','src/SeasonActivationLambda/serverless.template']) JSON.parse(fs.readFileSync(path,'utf8'));"

      - name: Build
        run: dotnet build VaultPreviewLambda.sln --configuration Release --no-restore

      - name: Run tests
        run: dotnet test VaultPreviewLambda.sln --configuration Release --no-build --no-restore
